[app]
name = "Whatomate"
environment = "production"
debug = false

[server]
host = "0.0.0.0"
port = 0

[database]
url = "${DATABASE_URL}"

[redis]
url = "${REDIS_URL}"

[jwt]
secret = "${JWT_SECRET}"
